# 2026-09-23

## 第八轮修复（对齐 + PDF 预览卡死）

1. **操作栏对齐**：根因是 @agentscope-ai/chat Sender 的 `.qwenpaw-sender-content-bottom`（底部操作栏）无 align-items，右侧附件按钮 32px 撑高行、胶囊 28px 顶对齐 → 胶囊上凸。修复：Chat/index.module.less 末尾加全局规则 `:global(.qwenpaw-sender-content-bottom){align-items:center}` + prefix/内层 antd Flex 也 center（规则编译进 index-ssDPdK1o.css）。
2. **PDF 预览关闭后卡死**：根因是 pywebview 的 WKWebView/WebView2 不支持 `<a download>`，ArtifactsPanel（最近产物）非 HTML 文件用裸 `<a href download>` → 点击后 webview 直接导航到文件 URL、SPA 被销毁无法返回 → 应用假死。修复三层：
   - 后端 desktop_cmd.py 新增 `preview_file(file_path)`（macOS `open`/Win `os.startfile`/Linux `xdg-open` 系统默认应用打开）；
   - 新增全局兜底组件 `console/src/components/DesktopFileLinkGuard/`（挂 App.tsx，capture 阶段拦截所有指向 /api/files/ 或 /files/preview/ 的 <a> 点击，桌面端 preventDefault → preview_file 优先、save_file 兜底；.html 例外走内置预览页）；
   - ArtifactsPanel/SendFileToUserRenderer 桌面端下载改显式 save_file 按钮，均新增「打开」按钮（preview_file）。
3. 类型注意：ArtifactsPanel 用有类型的 window.pywebview.api，save_file 返回 boolean，不能写 `=== "true"`（TS2367）；SendFileToUserRenderer 里是 `(window as any)` 所以历史上没报。
4. 打包（10:05 dist/小铁智友.app 419M）：preview_file（前端 JS + 后端 py）+ 对齐 CSS + 12 专家模板均已验证在包内。回滚备份 /tmp/capp_r8_old（昨晚 21:21 版）。磁盘剩 8.1G。
